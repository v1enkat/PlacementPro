<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile - CampusConnect</title>
    <style>
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .profile-container {
            background-color: #ffffff;
            border-radius: 15px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
            width: 90%;
            max-width: 800px;
            padding: 30px;
            display: flex;
            flex-direction: row;
            align-items: center;
            gap: 20px;
            position: relative;
        }

        .back-button {
            position: absolute;
            top: 15px;
            left: 15px;
            background-color: #4d4d4d;
            color: white;
            font-size: 0.9rem;
            padding: 8px 12px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
            text-decoration: none;
            display: flex;
            align-items: center;
        }

        .back-button:hover {
            background-color: #333333;
        }

        .back-button span {
            margin-left: 5px;
        }

        .profile-photo {
            width: 150px;
            height: 150px;
            border-radius: 50%;
            object-fit: cover;
            border: 4px solid #2b84f9;
        }

        .profile-details {
            flex: 1;
        }

        .profile-name {
            font-size: 2rem;
            font-weight: bold;
            color: #2b84f9;
            margin-bottom: 10px;
        }

        .profile-info {
            font-size: 1rem;
            color: #555555;
            margin: 8px 0;
        }

        .profile-info strong {
            color: #333333;
        }

        .buttons {
            margin-top: 20px;
            display: flex;
            flex-wrap: wrap;
            gap: 15px;
        }

        .button {
            padding: 10px 20px;
            font-size: 1rem;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
            flex: 1;
            min-width: 120px;
            text-align: center;
        }

        .button.update {
            background-color: #28a745;
        }

        .button.update:hover {
            background-color: #218838;
        }

        .button.logout {
            background-color: #dc3545;
        }

        .button.logout:hover {
            background-color: #c82333;
        }

        .button.company {
            background-color: #4d4d4d;
        }

        .button.company:hover {
            background-color: #333333;
        }

        @media (max-width: 600px) {
            .profile-container {
                flex-direction: column;
                align-items: center;
            }

            .profile-photo {
                margin-bottom: 20px;
            }

            .buttons {
                width: 100%;
                justify-content: space-between;
            }
        }
    </style>
</head>
<body>
    <div class="profile-container">
        <a href="/index" class="back-button">
            &#8592; <span>Back</span>
        </a>
        <img src="data:image/jpeg;base64,<%= user.image.toString('base64') %>" alt="Profile Picture" class="profile-photo">
        <div class="profile-details">
            <div class="profile-name"><%= user.name %></div>
            <div class="profile-info"><strong>Enrollment Number:</strong> <%= user.Enumber %></div>
            <div class="profile-info"><strong>Branch:</strong> <%= user.branch %></div>
            <div class="profile-info"><strong>Email:</strong> <%= user.email %></div>
            <div class="profile-info"><strong>Batch:</strong> <%= user.batch %></div>
            <div class="profile-info"><strong>Domain:</strong> <%= user.domain %></div>
            <div class="buttons">
                <button class="button update" onclick="window.location.href='/profile/update'">Update Details</button>
                <button class="button logout" onclick="window.location.href='/logout'">Logout</button>
                <button class="button company" onclick="window.location.href='/createcompany'">Update Company Details</button>
            </div>
        </div>
    </div>
</body>
</html>
