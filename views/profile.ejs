<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile - CampusConnect</title>
    <style>
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .profile-container {
            background-color: #ffffff;
            border-radius: 15px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
            width: 400px;
            padding: 30px;
            text-align: center;
            position: relative;
        }

        .profile-photo {
            width: 120px;
            height: 120px;
            border-radius: 50%;
            object-fit: cover;
            margin-bottom: 20px;
            border: 3px solid #2b84f9;
        }

        .profile-name {
            font-size: 1.8rem;
            font-weight: bold;
            color: #2b84f9;
            margin-bottom: 10px;
        }

        .profile-info {
            font-size: 1rem;
            color: #555555;
            margin: 8px 0;
        }

        .profile-info strong {
            color: #333333;
        }

        .buttons {
            display: flex;
            justify-content: space-between;
            margin-top: 20px;
        }

        .button {
            padding: 10px 20px;
            font-size: 1rem;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .button.update {
            background-color: #28a745;
        }

        .button.update:hover {
            background-color: #218838;
        }

        .button.logout {
            background-color: #dc3545;
        }

        .button.logout:hover {
            background-color: #c82333;
        }

        .footer {
            background-color: #2b84f9;
            color: #fff;
            text-align: center;
            padding: 10px;
            margin-top: 30px;
            border-radius: 0 0 15px 15px;
        }

        .footer p {
            margin: 0;
            font-size: 0.9rem;
        }
    </style>
</head>
<body>
    <div class="profile-container">
        <img src="data:image/jpeg;base64,<%= user.image.toString('base64') %>" alt="Profile Picture" class="profile-photo">
        <div class="profile-name"><%= user.name %></div>
        <div class="profile-info"><strong>Enrollment Number:</strong> <%= user.Enumber %></div>
        <div class="profile-info"><strong>Branch:</strong> <%= user.branch %></div>
        <div class="profile-info"><strong>Email:</strong> <%= user.email %></div>
        <div class="profile-info"><strong>Batch:</strong> <%= user.batch %></div>
        <div class="profile-info"><strong>Domain:</strong> <%= user.domain %></div>

        <div class="buttons">
            <button class="button update" onclick="window.location.href='/profile/update'">Update Details</button>
            <button class="button logout" onclick="window.location.href='/logout'">Logout</button>
            <button class="button " style=" background-color: #4d4d4d;" onclick="window.location.href='/createcompany'">Update Company Details</button>
        </div>

        
    </div>
</body>
</html>
